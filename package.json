{
  "name": "super-task",
  "private": true,
  "version": "1.0.0",
  "description": "Super Task local-first task board, packaged for Android via Capacitor.",
  "scripts": {
    "www:build": "node scripts/build-www.js",
    "cap:sync": "npm run www:build && npx cap sync android",
    "android:open": "npm run cap:sync && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "dependencies": {
    "@capacitor/app": "^7.1.2",
    "@capacitor/status-bar": "^7.0.6"
  }
}
